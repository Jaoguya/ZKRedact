# Build artifacts
/build/

# Experiment output — regenerated from config + seed, never committed
/results/

# Go
*.exe
*.test
*.out
vendor/

# ZK circuit artifacts (large; regenerated by make build-zk)
*.r1cs
*.zkey
*.ptau

# Fabric runtime state
network/crypto-config/
network/channel-artifacts/
*.block
*.tx

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/

# Fabric runtime state (generated by network/scripts/network.sh)
network/organizations/
network/channel-artifacts/
network/.topology
network/chaincode/*/vendor/
# compiled chaincode binary, produced by `go build` inside the module
network/chaincode/redaction/redaction

# Local venv for scripts/plot_figures.py (matplotlib)
/.venv-plot/
